# Application Name
spring.application.name=meliani-trans

# Server Port
server.port=${PORT:8080}

# PostgreSQL Database Configuration - Neon
# DATABASE_URL must use the JDBC format:
# jdbc:postgresql://<neon-host>/<database>?sslmode=require&channelBinding=require
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-wispy-haze-a2rr314o-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_OGot0J4ImSWA}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}

# JWT Configuration
jwt.secret=${JWT_SECRET:8f9a7c6e5d4b3a2910f8e7d6c5b4a3928172635445566778899aabbccddeeff1122334455667788}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
cors.allowed-origins=${CORS_ORIGINS:http://localhost:4200}

# Admin Default Credentials
admin.default.email=${ADMIN_EMAIL:Meliani@gmail.com}
admin.default.password=${ADMIN_PASSWORD:Meliani@123}

# Logging
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:INFO}
logging.level.com.melianitrans=${APP_LOG_LEVEL:INFO}
